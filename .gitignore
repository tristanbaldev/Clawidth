# Python / MicroPython
__pycache__/
*.pyc
*.mpy

# On-device runtime state (don't commit a personal rate / banked time)
state.json

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Firmware blobs (download per FLASHING.md, don't commit)
*.bin
*.uf2
